
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">

  <title>#(title)</title>
</head>

<body>
    <h1>#(title)</h1>
    #if(count(numbers) > 0):
        There are #count(numbers) numbers<br>
        #for(number in numbers):
            <p>#(number)</p>
        #endfor
    #else:
        There are no numbers yet :(
    #endif
</body>
</html>

    


